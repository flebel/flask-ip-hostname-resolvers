Two Flask apps I've been using for a while to self-host my external IP and
hostname resolvers over HTTP.

The Flask apps have been rewritten as pure WSGI apps:
https://github.com/flebel/wsgi-ip-hostname-resolvers

